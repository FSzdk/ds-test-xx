# ds-test-xx

Semantic components built on @ariakit/react. Tokens live in tokens/tokens.css
and are consumed via CSS custom properties in each component's .module.css.

This repo is source-only — not published, no build step required to read it.
